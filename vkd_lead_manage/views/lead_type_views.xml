<odoo>
    <record id="crm_lead_type_view_tree" model="ir.ui.view">
        <field name="name">crm.lead.type.view.tree</field>
        <field name="model">crm.lead.type</field>
        <field name="arch" type="xml">
            <tree string="Lead Types" editable="bottom">
                <field name="name"/>
                <field name="active" optional="show"/>
            </tree>
        </field>
    </record>

    <record id="action_crm_lead_type" model="ir.actions.act_window">
        <field name="name">Lead Types</field>
        <field name="res_model">crm.lead.type</field>
        <field name="view_mode">tree</field>
    </record>

    <menuitem
        id="menu_crm_lead_type"
        name="Lead Types"
        parent="crm.menu_crm_config_lead"
        sequence="5"
        action="action_crm_lead_type"
        groups="sales_team.group_sale_manager"/>
</odoo>